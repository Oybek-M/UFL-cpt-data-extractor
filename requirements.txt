# UFL — pinned dependencies (Docker image ichida reproducible build uchun)
# Yangilash: versiyani shu yerda o'zgartirib, `docker compose build` qiling.

# --- CLI / config / logging ---
typer==0.26.8
rich==13.8.1
pydantic==2.9.2

# --- Ingest: PDF / OCR ---
pymupdf==1.24.10
pytesseract==0.3.13
pillow==10.4.0

# --- Ingest: EPUB / DOCX / FB2 / HTML ---
ebooklib==0.18
beautifulsoup4==4.12.3
python-docx==1.1.2
python-pptx==1.0.2
lxml==5.3.0
trafilatura==1.12.2

# --- Til aniqlash ---
fasttext-wheel==0.9.2

# --- Tokenizer (Gemma-4; torch shart emas — faqat tokenizatsiya) ---
# 4.44.2/0.19.1 Gemma-4'ning tokenizer.json formatini o'qiy olmaydi ("ModelWrapper" xatosi) — yangilangan.
transformers==5.14.1
tokenizers==0.22.2
sentencepiece==0.2.0
huggingface-hub==1.24.0

# --- Dedup ---
datasketch==1.6.5

# --- Web UI (Faza 3) ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
jinja2==3.1.4
python-multipart==0.0.9

# --- Test ---
pytest==8.3.3
httpx==0.27.2
